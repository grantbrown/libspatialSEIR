PKG_CPPFLAGS=-I../../../eigen ./LSS/include ./LSS/include/DataStructures ./LSS/include/FullConditionals 

SOURCES=$(wildcard ./*.cpp LSS/src/*.cpp LSS/src/FullConditionals/*.cpp LSS/src/IterationTasks/*.cpp LSS/src/Samplers/*/*.cpp)

OBJECTS = $(SOURCES:.cpp=.o)

all: $(SHLIB)

clean:
	@rm -f $(OBJECTS)
